import Button from '@mui/material/Button';
import Tooltip from '@mui/material/Tooltip';

export const ArrowTooltips=()=>{
  return (
    <Tooltip title="Add" sx={{color : "white"}}>
      <Button>ADD</Button>
    </Tooltip>
  );
}
