import * as React from 'react';
import Button from '@mui/material/Button';

export default function BasicButtons() {
  return (
      <Button variant="contained" sx={{height : "30px", marginLeft : "20px"}}>Add</Button>
  );
}